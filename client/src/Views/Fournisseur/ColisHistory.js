// ColisHistory.js

import React from 'react';

const ColisHistory = ({ colis, onClose }) => {
  // Implement logic to display the colis history
  // You can fetch the history data from your backend API based on the colis ID

//   return (
//     // Create a component or layout to display the colis history
//     // You can use tables, lists, or any suitable UI for the history
//     // Add a "Close" button to close the modal
//   );
};

export default ColisHistory;
